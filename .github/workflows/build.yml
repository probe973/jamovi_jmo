name: Build jamovi module

on:
  push:
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v4

      - uses: r-lib/actions/setup-r@v2

      - name: Build jmo
        shell: Rscript {0}
        run: |
          options(repos = c(
            CRAN = "https://cloud.r-project.org",
            jamovi = "https://repo.jamovi.org"
          ))

          install.packages(c("jmvcore", "jmvtools"))

          jmvtools::install(".")

          lib <- .libPaths()[1]
          jmo_files <- list.files(
            path = file.path(lib, "jmv"),
            recursive = TRUE,
            pattern = "\\.jmo$",
            full.names = TRUE
          )

          file.copy(jmo_files, getwd(), overwrite = TRUE)

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: jamovi-module
          path: "*.jmo"
